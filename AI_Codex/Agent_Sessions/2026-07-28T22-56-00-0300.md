---
timestamp: 2026-07-28T22:56:00-03:00
project: codex-workflows-plugin
branch: master
status: active
next: null
tags: [agent-session, installer-bootstrap-import]
---

# Agent Session

## Carried forward
Previous session (2026-07-25): session gate / skip-ledger / protected-branch git guards (next: null).

## Current Intent
Investigate and fix ModuleNotFoundError: No module named 'scripts' during curl|bash install.sh wiring (bootstrap.py wire → purge_markdown_allowlist import).

## Findings
- Root cause: `wire()` imported `scripts.installer.purge_markdown_allowlist` before adding `install_dir` to `sys.path`.
- `install.sh` extracts `bootstrap.py` to `/tmp` and runs it standalone → no package context → `ModuleNotFoundError: No module named 'scripts'`.
- Zip extract to `.codex-workflows` succeeded; failure was only in the wiring phase.

## Fix
- Added `_ensure_install_import_path()` and call it before any `scripts.*` imports in `wire()` / `main()`.
- Install zip (when provided) before uninstall/purge imports for standalone bootstrap.
- Regression: `TestStandaloneBootstrapZipInstall.test_standalone_bootstrap_wires_from_zip`.
- Branch: `bugfix/bootstrap-standalone-scripts-import`
