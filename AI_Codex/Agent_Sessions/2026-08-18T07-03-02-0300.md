---
timestamp: 2026-08-18T07:03:02-03:00
project: codex-workflows-plugin
branch: techdebt/ledger-housekeeping-core-plan-v3
status: active
next: null
tags: [agent-session, vault-split, pointer]
---

# Agent Session 2026-08-18T07-03-02-0300

## Intent

`airlock` now has its own ledger at `../airlock/AI_Codex/`. Its specs, plans,
reference notes and session logs moved there.

**This session exists for a mechanical reason, not a workstream one.** The
enforcement hook resolves the project root from `CLAUDE_PROJECT_DIR`, which
points at `codex-workflows-plugin`, so it only ever looks for an open session in
*this* vault. Without one here, writes anywhere — including into `airlock` —
are denied.

The real record of airlock work lives in `../airlock/AI_Codex/Agent_Sessions/`.
Once a session is launched from the airlock directory this session can be closed
and the duplication ends.

## Next
