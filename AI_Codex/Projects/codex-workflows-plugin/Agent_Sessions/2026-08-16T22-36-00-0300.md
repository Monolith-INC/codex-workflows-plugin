---
timestamp: 2026-08-16T22:36:00-03:00
project: codex-workflows-plugin
branch: techdebt/ledger-housekeeping-core-plan-v3
status: active
next: 2026-08-17T09-48-14-0300.md
closed_at: 2026-08-17T09:48:14-03:00
tags: [agent-session, ledger-housekeeping, agent-harness-core, extraction-plan]
---

# Agent Session 2026-08-16T22-36-00-0300

## Carried forward

Twelve Agent Session records across `AI_Codex/Agent_Sessions/` and
`AI_Codex/Projects/codex-workflows-plugin/Agent_Sessions/` were left open
(`next: null`), the oldest dating to 2026-06-24. The backlog accumulated
because `AI_Codex/.codex_ledger_skip` was set on 2026-07-29 (reason recorded:
*"user requested investigate/fix installer ModuleNotFoundError"*) and masked
the session gate for 18 days.

Prior workstream: agent-harness-core extraction design. Three drafting
artifacts exist; see `Implementation_Plans/`.

## Current Intent

1. Clear the ledger skip flag and restore enforcement. **Done** — flag removed
   via `disable_ledger_skip('AI_Codex')`.
2. Close the twelve stale sessions by chaining each `next:` to its
   chronological successor. Non-destructive: no session file is deleted.
3. Open this session as the continuation point.
4. Relocate `2026-08-16-agent-harness-core-HANDOFF.md` out of
   `Agent_Sessions/` — it has no session frontmatter and misreports vault state.
5. Draft implementation plan **v3**, correcting the defects found when plan v2
   was checked against `<source>` @ 0.5.20.

## Findings carried into v3

- `orchestrator-component-architecture.md` does not exist anywhere in the
  workspace, despite being cited as foundational by all three drafting
  artifacts. Its invariants table has been reconstructed from code with
  `file:line` anchors.
- `scripts/policy/engine.py` contains no separable rule dispatcher. Plan v2
  Task 3.1 is unexecutable as written.
- `get_project_root()` (`scripts/hook_runtime.py:57`) and the project-root
  resolver in `scripts/orchestrator/handlers.py:19` diverge in env-var
  precedence, fallback semantics, and return type. Open Q7 has an answer:
  they are not the same function.
- Installer exposes 8 `Target` enum members, 6 of which write config paths.
  Plan v2's "six targets" parity gate needs pinning.

## Verification baseline

`python3 -m unittest` → **233 tests, OK, 8.3s** (pre-change, on `main` @ 78fad01).

## Constraints in force

- **Implementation Lock:** `IMPLEMENTATION APPROVED` not issued. Drafting only;
  no code relocation this session.
- **Codex Inviolability:** no deletions. Supersession is marked, not removed.

## Outcome

- Ledger skip flag removed; `evaluate_session_gate` → `active=True`.
- Twelve stale sessions closed by chaining `next:` to successors; `closed_at` stamped.
  No session file deleted.
- Handoff relocated to `Implementation_Plans/2026-08-16-agent-harness-core-HANDOFF.md`.
- Plan **v3** drafted: `Implementation_Plans/2026-08-16-agent-harness-core-extraction-v3.md`.
- Plan v2 marked `DEPRECATED`; design v2 marked `CORRECTIONS ISSUED`. Neither deleted.

Headline v3 change: the inbound execution ring (`hook_runtime.py`, `policy/engine.py`)
is deferred to Phase 2 because neither file can be relocated without dragging tenant
logic — ticket lifecycle and YouTrack — into a vendor-agnostic core. Milestones are
reordered by tenant coupling ascending, so the installer (zero coupling, 2,562 LOC)
lands first and the disputed ring is never touched in Phase 1.

## Amendment — 2026-08-17

`orchestrator-component-architecture.md` was supplied by the user and now sits in
`Implementation_Plans/`. It was not in the working tree when v3 was drafted
(created 2026-08-17 05:17; the directory was listed twice on 2026-08-16 without it).

**Process failure to carry forward:** on finding a foundational document missing, the
correct move was to stop and ask, not to reconstruct its content and build a plan
around its absence. The absence was reported but never posed as a decision. Ask next
time — the cost of a question is far below the cost of rework.

v3 reconciled against it:
- Invariant count 7 → **9**. Added *discovery is filesystem-driven*
  (`manifests.py:9`) and *input validation runs twice* (`engine.py:83`, `worker.py:23`).
  The second appears in no prior artifact, including the architecture document's own
  invariants table.
- Milestone 0 gains Task 0.5 for both; carve maps renumbered to 0.6.
- §1.4 reframed: the document endorses consolidating host resolution; v3 disputes only
  the phase, not the destination.
- §2 now answers the document's governance-symmetry argument head-on — deferring the
  inbound execution ring means core ships one of the two authorization halts.
- New Phase 2 item: injected configuration. `mcp_server.py:17` reads
  `ORCHESTRATOR_SKILLS_DIR` and is core-destined — env-read config crossing the
  boundary in Phase 1.

§1.2 — the central finding — is untouched. It came from reading code, and the
architecture document deliberately excludes extraction sequencing.

## Next
