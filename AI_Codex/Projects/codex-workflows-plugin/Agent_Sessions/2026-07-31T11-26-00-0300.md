---
date: 2026-07-31
time: 11:26:00-0300
branch: main
next: null
---

# Agent Session 2026-07-31T11-26-00-0300

## Previous
Interactive install TTY fallback / "no interactive terminal available" on main (session 2026-07-31T01-51-53-0300).

## Intent
Fix Cursor hook `ModuleNotFoundError: No module named 'adapters'` by putting `scripts/` on `sys.path`, switching `hook_runtime` to bare package imports, and ensuring Cursor `emit_decision` always emits JSON (failClosed).

## Continuation — 2026-07-31T11-32 (approx)
**Branch:** main (dirty from Cursor hook fix; no new commits for this cleanup)
**Intent:** Optional cleanup — migrate hybrid `from scripts.*` intra-package imports under `scripts/` (adapters + related runtime) to bare-package convention (`from policy…`, etc.), matching `hook_runtime.py`. Keep entrypoint/`python -m` REPO_ROOT imports as `from scripts.…`. Non-destructive; no commit unless asked.

## Outcome — bare-import cleanup
Migrated adapters + related runtime modules under `scripts/` from hybrid `from scripts.*` to bare packages (`from policy…`, `from artifact_reflection…`, etc.). Added `SCRIPTS_DIR` path bootstraps on adapters/orchestrator/artifact_profiles and several top-level runtime modules. Left intentional `scripts.` prefixes on hook entrypoints, installer (`python -m scripts.installer…`), orchestrator `__main__`, and tests. Full suite 231 OK; Cursor hook smoke allow/exit 0.

## Continuation — 2026-07-31T12-05
**Branch:** main (dirty worktree; no commit unless asked)
**Intent:** Fix Cursor MCP `agentic-orchestrator` `ModuleNotFoundError: No module named 'scripts'` when Cursor spawns `python3 -m scripts.orchestrator.mcp_server` without plugin root on PYTHONPATH/cwd. Prefer durable plugin launcher + MCP args/env using `${CLAUDE_PLUGIN_ROOT}` (Cursor expands both CLAUDE/CURSOR tokens).

## Continuation — 2026-07-31T12-29
**Branch:** main (dirty; no commit unless asked)
**Intent:** After project-hook lockout restore, finish marketplace Cursor hooks (`hooks/hooks-cursor.json` + `.cursor-plugin/plugin.json`), keep project `.cursor/hooks.json` on relative path only, re-verify MCP launcher + all harness installer wiring/tests.

## Outcome — harness settings + MCP (2026-07-31T12-30)
- Project `.cursor/hooks.json` kept on relative path only (no CURSOR_PLUGIN_ROOT).
- Added marketplace `hooks/hooks-cursor.json` with `${CURSOR_PLUGIN_ROOT}`; `.cursor-plugin/plugin.json` points there.
- Codex project installs write `.codex/hooks.json`; Gemini/Antigravity-CLI timeouts use ms (5000).
- MCP launcher works from non-plugin cwd; full unittest suite OK.
