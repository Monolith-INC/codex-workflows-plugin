# Architecture protocol

Before implementation, retrieve the mapped work item and relevant tracker artifacts through the generic integration contract. Record design decisions as tracker artifacts. Do not assume a vendor or client-specific state model.
