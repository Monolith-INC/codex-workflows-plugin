# Git workflow

- Use the branch convention selected during bootstrap; `{key}` must identify exactly one tracker work item.
- Keep protected integration branches clean and use pull requests through the configured SCM adapter.
- Synchronize an ancestor branch into descendants before new commits when using stacked stories.
- Run verification before publishing or completing a pull request.
